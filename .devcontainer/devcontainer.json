{
  "name": "Ethereum C Dev (x86_64)",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "apt update && apt install -y git build-essential autoconf automake libtool pkg-config libssl-dev && bash build.sh"
}
