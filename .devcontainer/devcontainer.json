{
  "name": "C + Ethereum",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "apt update && apt install -y autoconf automake libtool pkg-config build-essential libssl-dev git && bash build.sh"
}
